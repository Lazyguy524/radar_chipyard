# 项目剖析要求与延续记忆

## 2026-09-17 当前优先级：先完成软件收敛

用户明确暂时无法提供硬件测试资源，已批准执行固定软件收敛计划。**本节优先于下方历史记录中“下一步 SoC/板测”的安排。** 暂停新增 RTL、SoC、位流和板测，不把硬件可用性作为软件工作前置条件。

- 最终结果：12 条训练和全条件评价已完成，四候选均未通过全部门槛，保留 A 主基线/B 困难条件备选。真实覆盖有条件收益，均值修正未建立全面替代优势；固定权重精度结论绑定具体模型。1,655,508 次主/备三种子完整 C 对拍通过，旧资料清单另经 SHA 核验。
- 当前入口：`software_convergence_2026-09-17/`。恢复真实少点、两种表示×两种覆盖×三个 seed，固定 12 条训练；保持 21→64→32→2、既有交叉熵和优化器，无新增蒸馏、LSTM、删维或结构搜索。
- 原主问题不变：点数/形态变化下的近似误差、表示/精度与共享成本。21 维只是控制变量，当前板卡有资源余量，不虚构资源不足。
- 用户接受兼顾常规与困难条件的新平衡门槛；所有旧门槛及失败结论保持历史原状。真实单点/两点必须单列，既有验证域仍属软件开发数据，不是独立泛化确认。
- 12 条结束后继续归因、选型、C 软件包、论文记录、Git 备份与 Obsidian 同步；不追加训练救分。不通过则保留 A 主基线、B 困难条件备选。
- 训练前已在 `backup/radar-thesis-20260917-software` 独立分支备份，原工作分支和 index 保持。最终状态以新包 README、validation.json 和备份回执为准。


## 2026-09-17 持续推进与代表方案 RTL 衔接

用户指出停止某一轮训练不该导致整个项目停止，批准继续形成“收益/代价→实现→匹配板测”的证据链。用户已疲惫，面向用户的总结应短、白话、先说完成了什么，避免让用户重新学习术语。

- 新入口：[代表方案硬件衔接](representative_rtl_2026-09-17/README.md)。未新增训练；A/B/C 固定原列表首个 seed 7 进行实现验证，不按分数挑种子；科学比较仍保留原三 seed，D 负结果不删。
- 先 Python 检查，再复制真实 Chisel 源到独立 package/目录，匹配逐特征移位和模型参数。旧统一定标输出存在信息丢失，不能只换权重或在旧 INT8 后换算。
- 三种实际 Feature21→QMLP RTL 均通过每模型 80,450 个真实簇＋542 个边界簇，共 242,976 次模型—点簇对拍，含特征、padding、logits、阻塞与取消恢复。编译工具问题及修正保留记录；原生产 RTL/发布权重/位流未改。
- 本轮是 standalone RTL 功能 PASS，未产生完整 SoC、位流、板测，也不等于算法通过旧推广门槛。带人为阻塞的仿真周期不得用于最终性能指标。
- 后续完成独立 SoC 接入和同配置板测；独立质量确认数据仍有缺口。不继续无边界调参，不因这次验证恢复暂缓的 Gemmini 板测。
- 本轮独立备份分支为 `backup/radar-thesis-20260917-rtl`，提交/远端/归档/本地 Obsidian 状态以新包回执为准。旧工作分支与暂存区保持，原三个冻结研究包需要按清单核验。

## 2026-09-16 用户要求的主线保护与最新实验收口

用户批准继续两种受约束训练，要求反思、反馈、保存设计思想，并用 Git 备份。当前记录见[条件保护训练](conditional_protection_2026-09-16/README.md)与[设计账本](conditional_protection_2026-09-16/00_设计账本与主线.md)。研究结论允许被证据修订，原主问题和历史理由不能静默改写。

- 六条完整训练已经结束，两种方法均未通过全部预定门槛；正确判断保护减少负翻转并缓解部分组退化，但仍存在形态条件代价。不要在接续时误读为方法已经推广或实验未执行。
- 冻结普通训练 A、扰动训练 B、正确判断保护 C 为代表对照，D 负结果保留；当前不继续增加损失系数/seed。后续优先评价闭环、尺度/参数/位流实现闭环和论文证据链。
- 保护旧正确判断与分组风险优化均有直接既有文献，不能单列为原创算法。21 维仍是控制变量，不是证明最优。
- 历史二分类清单为 train 113、validation 27、test 13 个序列；额外五个本地序列没有当前目标类别的有轨迹点。本轮无新增历史 test 评分，无独立确认结论。
- 最近研究文件原先多未提交。本轮创建 `backup/radar-thesis-20260916-protection` 选择性备份分支，原工作分支 HEAD/index 不改；源码、设计、模型与小型结果进 Git，大数组/本轮预测另归档。实际提交号和远端回读以[备份回执](conditional_protection_2026-09-16/04_备份与复现.md)为准，不将工作目录、Git 提交、远端推送、Obsidian 云同步混为一谈。
- 用户原研究问题仍是点数/形态变化下的近似误差、表示/精度与共享实现质量—成本取舍；不把主线改成纯蒸馏/纯分类分数竞赛。原权重/位流保留，最终性能以上板对应配置实测为准。


## 2026-09-16 持续测试后的最新接续状态

用户要求继续有约束地测试，积累足够数据后再判断，不要快速结束汇报。现已在独立目录完成 35 条新固定轮数轨迹（20 机制＋9 扰动训练＋6 编码），每条完整 train、20 FP32＋60 QAT，不选最佳验证轮次/seed。当前入口为[机制收敛研究](mechanism_convergence_2026-09-16/README.md)，额外[编码专项](moment_encoding_2026-09-16/README.md)另有冻结计划。

- 原主问题继续有效，不能在记忆压缩后改成单纯跑 RF、删到某个维数或先做 RTL。
- 统计修正不具普遍优势；输出方差大量映零属真现象，开平方恢复信息但未带来全面分类优势。强简单基线必须保留。
- 扰动训练改善少点/形态变化，且改善自然被过滤观测；但近距离、历史充分组会回退，不能宣称全面更稳健。关键组约束比再增加几个算子更值得优先研究。
- 原粗输出下 Q12 过门槛不能外推到细输出；统一 Q16 当前数值门槛通过，尚无新物理成本证据。已发布参考 C 全是明确 Q24。
- 35 个模型、11 条件、真实被过滤 97,734 观测、五档倒数和完整 C 已完成。两个计划的训练预算到此结束，不为救某个假设继续追加 seed/表示。下一步先冻结独立确认与关键组保护协议，不在熟悉 validation 上无止境选方案。
- 旧数据/权重/RTL/位流保留，未访问远端训练机、未启动 GPU/RTL/板测。最终板测性能标准仍按用户原要求执行。

以下“六条训练后的下一轮”等段落是旧阶段记录；最新决策见[评审与收敛决定](mechanism_convergence_2026-09-16/03_评审与收敛决定.md)。

## 2026-09-16 研究主问题与防偏移约束

用户明确要求围绕以下问题实现并收敛算法：**在点数和目标形态变化时，哪些近似会损害分类稳定性？能否针对这些误差，用更合适的统计表示、精度分配和共享计算结构，获得优于现有实现的质量—成本取舍？**

- 此问题是当前算法主线。21 维、某个分类器和删维数量都不是预先指定的最终答案。
- 冻结尺度 QAT 与整数导出是公平对照的基础；网络训练进入平台期不等于上述算法研究已经收敛。
- 随机森林、特征重要性和消融属于解释与交叉检验工具，不替代点数/形态条件误差、表示/精度对照与共享计算成本分析。
- 优先区分观测变化、统计定义替换、有限精度；用同簇配对干预与自然序列分层互补，不把相关性或模拟减点当成真实雷达物理因果。
- 简单定标和部署匹配训练必须作为强基线；若它们已解释收益，不增加不必要的统计算子或控制逻辑。
- 成本以共享算子及位宽依赖初筛，最终以上板对应配置的实测为准；不能用少几个特征或少若干理论运算替代实际资源/时序/能耗。
- 执行继续保持预先预算、Python 预检、负结果、原产物保护和独立输出目录。进展见[条件统计研究](conditional_statistics_2026-09-16/README.md)。后续会话先复核本节，再判断新增任务是否服务主问题。

本轮进展已冻结：三种新表示×两个 seed、五种配对输入条件，补齐两个固定 RF、组置换及原始过滤统计；已有单遍 C 统计与完整 C 分类候选。简单逐特征定标为强基线，矩统计只有条件性改善线索；Q16 重训方案未过门槛且到训练上限，固定 Q24 权重单独降为 Q16 则几乎不改预测，二者不可混为精度因果。单点/两点观测历史排除比例 train 57.0163%、validation 54.8500%，不因此默默更换数据集或扩大稀疏目标结论。当前没有证明 21 最优或最终硬件优势；下一轮首先隔离均值与空间二阶矩的必要性，见[阶段评审](conditional_statistics_2026-09-16/03_评审与下一步门槛.md)。旧临时“先做森林筛选”的排序由本段复核取代，原文保留为历史。

## 2026-09-15 用户最新决定

- **用户明确：最终性能标准按上板实测确定。** Python/RTL 周期、趋势和比值仅作设计评估，不作为最终性能基准，不填入最终板测性能表或摘要/结论中的实际加速收益。时序通过只是实现条件；最终资源—性能数据须绑定同一位流、ELF、工作负载、时钟和板端日志。详见[指标使用规则](gemmini_qmlp_comparison_2026-09-15/06_板测优先与指标使用规则.md)。
- 已完成 [Gemmini/QMLP RTL 设计评估](gemmini_qmlp_comparison_2026-09-15/README.md)，**最终同条件板测比较尚未完成**。原生 Gemmini16 与 QMLP8 在相同 RTL SoC 中，batch=1/8/32 各对拍首 32 帧。568 cycles/sample 属于原生 Scale 版 RTL，该版 FPGA 时序失败；4628.84375 属于无 Scale、CPU 后处理版 RTL，该版位流时序通过但未上板。两者均非最终指标；不预先指定原生版为最终比较基准。既有 QMLP 板测保留其原始适用范围，不与 Gemmini 仿真计算最终加速比。
- 训练机开机后的资料补齐请求已保存于 [新对话材料](training_and_gemmini_retest_2026-09-15/01_训练机资料补齐对话.md)。当前是训练溯源缺口，已有软件参数、指标和 golden 不应被描述为不存在。
- 停车场案例和新增现场采集暂缓，不作为本轮实验前置条件。
- 对当前紧凑配置，Nexys Video 仍有明显资源余量；不得笼统宣称“板卡资源耗尽”。用给定平台、输入规模、资源预算与性能指标描述设计取舍。
- 用户明确新授权 Gemmini 独立复测和比较，由一个 Luna 执行、主代理监督；这是历史不做 Gemmini 规则的当前例外。新增比较路线的 RoCC 接口可如实说明，原自定义算术 ISA 不恢复为原创贡献。
- 所有原有未板测 bitstream、最新版成果、失败证据均保留；新实验使用独立配置、JAR、生成目录和交付包。
- 初轮 4×4/K21 真实 RTL 对拍通过；33.333333 MHz Gemmini 物理实现通过已约束路径时序（WNS +0.758 ns / WHS +0.008 ns），旧 50 MHz 候选失败独立保留。该初轮尚未做完整 QMLP 比较，后续进展见本节首项；历史证据见[复测结果](training_and_gemmini_retest_2026-09-15/04_Gemmini复测结果.md)。
- 当前优先阅读顺序及新增两条文献见 [阅读说明](training_and_gemmini_retest_2026-09-15/05_现在优先读哪些论文.md)。Scheiner IV 2018 最贴近固定维度雷达特征；不是用外部论文替本项目证明 21 维最优。

Date: 2026-08-26

## 2026-09-15 Scheiner 读后讨论与暂缓事项

- 用户要求暂缓补齐 Gemmini/QMLP 板测套件，先[记录待办](scheiner_feature21_review_2026-09-15/00_暂缓的Gemmini板测任务.md)。不因本轮论文讨论自动启动构建或板测。
- [新核对包](scheiner_feature21_review_2026-09-15/README.md)区分论文候选、软件 21 维与部署近似代理；21 维是当前冻结表示，不能写成已由 50 维筛选得到的最优子集。
- RadarScenes 官方将 Scheiner IV 2018 列为早期子集研究；不能称双方数据完全无关，也不能据此声称相同样本和划分。
- 二分类指标较好不等于无偏。完整软件测试与均衡板测子集分列；细类合并规则、K7 划分泄漏检查及部署表示消融待补。论文说明任务边界与实际错误，不隐藏影响结论的限制。

## 用户目标

2026-09-15：用户授权从 `student@172.31.102.72:10022` 恢复资料并做有限尝试，考虑单 RTX 3090 实际占用。专用公钥已认证成功，335 个原文件取回并逐项 SHA256 校验。随后三次新连接超时，ping 收到主机不可达；用户最新要求今晚停止重试，仅做本地工作，明天开机后再排查。专用私钥仅在用户 SSH 目录，不进入仓库或共享笔记。状态、数据与接续对话见[恢复包](training_server_recovery_2026-09-15/README.md)。

本轮 CPU 复现已核：完整样本身份、14/18/21 数据关系、三个 checkpoint→train 校准→冻结部署参数、全 test 原 HDF5 特征与整数推理；新全量部署镜像 Macro-F1=0.963226 属历史 test 软件评价，不能当新板测或未触碰的泛化成绩。确认历史 test 用于选 K、QAT eval 仍更新尺度、K7 无时间上限且未做跨帧坐标对齐；需优先完善评价协议。最终冻结导出独立使用完整 train 校准，不应误判成 test 校准。未从头训练、未用 GPU，原模型/位流不改。

用户明确愿意调整工程以优先排除重大逻辑漏洞；这不要求未经核实就全部推倒。按[逻辑闭环清单](training_server_recovery_2026-09-15/01_逻辑闭环与修改范围.md)区分资料缺失、论证过强、评价错误、实现错误和研究前提问题。21 维可保留为部署基线；少量删维的资源收益未经证明，消融首先用于信息贡献分析。Gemmini 板测仍暂缓。

本项目是研究生毕业课题，需要从工程可运行状态提升到“能彻底解释、能写论文、能画图、能答辩”的知识结构。后续优化仍然重要，但当前阶段优先做项目剖析和论文知识库。

## 内容要求

- 必须解释数据输入是什么、规格大小是什么、是否已经定型、数据类型和位宽是什么。
- 必须解释不同输入类型或不同处理路径是否有区分设计。
- 必须解释数据从输入到输出经过哪些步骤，每一步数据内容、类型、shape、位宽如何变化。
- 必须解释标准 RISC-V 软件如何通过 MMIO、DMA IRQ、PLIC、machine trap 和 `WFI` 调度加速任务，并区分标准 ISA、常见平台组件和 Rocket 实现特有机制。
- 必须解释整体数据流通路：CPU、DDR、DMA、Feature21、QMLP 之间怎么连，为什么需要 TileLink、AXI4、AXI4-Lite、AXI4-Stream 等协议。
- 必须深入解释 Feature21 和 QMLP 内部细节：状态机、定点化、pipeline、关键寄存器、周期、吞吐、瓶颈和误差。
- 必须分析是否数据搬运足够快，是否让计算空拍，哪些 profile 可以支持该判断。
- 必须整理相关论文和类似架构对比，但不能用外部论文结论替代本项目证据。

## 写作和维护偏好

- 中文优先。英文仅用于专有名词、文件名、模块名、指令名和论文/工具名。
- 结论必须带证据路径；不写“感觉上更快”这种无法追踪的表述。
- 性能数字要标明口径：算法指标、DMA timed interval、validation e2e、Feature21-only、QMLP-only、full-chain、FPGA board、ASIC pre-layout 等不能混写。
- 2026-08-26 起，当前论文、答辩、摘要、章节大纲和创新点不再使用自定义算术指令、custom ISA 或 RoCC 局部加速路线。相关 RTL、日志和历史文档只作为工程档案保留，不进入当前主线证据与结论。
- 当前 RISC-V 主线是 Rocket/Chipyard 平台、标准特权机制与 `WFI`、machine trap，以及基于 PLIC 的 DMA 中断。PLIC/CLINT 是常见 RISC-V 平台组件而非基础 ISA；`mcache_ctl@0x7c1` 是 Rocket 实现特有 CSR，必须明确写成非标准机制。
- 旧 `docs/paper/` 和已退出主线的历史文档可以追溯，但不能覆盖当前 Feature21/QMLP、FPGA、ASIC 与 IRQ/WFI 证据。
- Obsidian 文件要使用 `[[WikiLink]]` 串主题，repo 文档要使用相对 Markdown 链接串证据。
- 图纸描述要尽量中文化，并保留 Visio COM 可读的 JSON 结构，方便 Windows 下自动画图。

## 固定入口

- Repo 事实源：`/home/soooarr/chipyard/docs/paper_new/README.md`
- Obsidian 主入口：`/home/soooarr/obsidian/项目/毕业论文——研究生/Chipyard论文知识库/Chipyard雷达SoC论文知识库.md`
- Obsidian 辅助索引：`/home/soooarr/obsidian/Codex/Chipyard/Chipyard Index.md`
- 项目专用 skill：`/home/soooarr/.codex/skills/radar-paper-knowledge/SKILL.md`

## 后续窗口规则

1. 先读 `docs/paper_new/README.md`，再按任务进入对应章节。
2. 写论文或答辩材料时，先查 `12_experiment_evidence_matrix.md` 和 `27_thesis_closeout_audit_2026-08-26.md`，避免用旧数字或已退出的叙事。
3. 讨论 RISC-V 时优先说明标准 `WFI`、CSR/trap 语义和开放 SoC 集成便利性；不得把 PLIC、CLINT 或 Rocket `mcache_ctl` 误写成标准基础 ISA。
4. 讨论板级测试时默认 `/dev/ttyUSB0`、`115200`、每个 fresh ELF 前按 `CPU_RESET`。
5. 所有性能与功耗数字必须记录对应源码、ELF、bitstream/网表、报告和日志 SHA；禁止混用不同设计版本、工作负载或测量窗口。
6. 更新 repo 文档后，同步 Obsidian 镜像入口或主题页；至少更新主入口的“最近更新”。
7. 新增图时同时维护 `pic/*.visio.json` 和 `pic/architecture_diagrams.md`。


## 2026-09-12 论文写作与文献延续

- 最新写作入口：[论文凝练与文献包](thesis_writing_literature_2026-09-12/README.md)。新材料细化六章、三级目录和三个研究问题，并区分方法贡献候选与工程工作。
- 已找回 Obsidian 个人《论文细节》《初定大纲》《开题报告内容要求》；师兄的参考文献数、页数和阅读报告要求仅作经验来源，未确认学校统一硬指标。
- 当前任务是已形成雷达点簇的 pedestrian/vehicle 分类；停车场为应用动机，现有 PC replay 不构成停车场现场采集或完整检测跟踪系统证据。
- 用户希望文献可直接导入 Zotero。书目先核题名/作者/年份/出版状态/DOI，再导出 CSL JSON、RIS、BibTeX，三种只导入一种；同一研究的预印本和正式版不要重复计数。
- Obsidian 不应只有 Linux 绝对路径：新写作材料与核心附件复制到共享 vault，使用 WikiLink，并保留仓库事实源及阅读快照身份。本地写入完成与远端同步完成分别记录。
- HPM4 的实施后状态以 pmu_execution_2026-09-12 冻结包为准；新写作材料不能把此前 PMU“建议实施”当最新进度，也不能把真实 RTL 校准/FPGA 物理实现写成新镜像已上板。
